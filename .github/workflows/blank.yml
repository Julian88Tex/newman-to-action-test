name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    # - name: Install Newman
    #   run: |
    #     npm install newman
    - name: Run Collection
      uses: postman/newman
      run: |
        newman run collections/Google\ Places\ API.postman_collection.json